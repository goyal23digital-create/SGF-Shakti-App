/* Entry for the single-file artifact/deploy build: same app, plus fonts
   inlined as data URIs since the artifact host blocks external requests. */
import './styles/fonts-inline.css';
import './main';
